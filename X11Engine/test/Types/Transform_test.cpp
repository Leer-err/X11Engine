#include "Transform.h"

#include <gmock/gmock.h>
#include <gtest/gtest.h>

TEST(TransformTest, ToMatrix) {
    // Transform tr = {};

    // tr.position = Vector3{1.0, 2.0, 3.0};
    // tr.scale = Vector3{2.0, 1.0, 1.5};

    // Matrix result = tr.toMatrix();

    // Matrix expected = Matrix::identity();

    // expected.matrix[0][0] = 2;
    // expected.matrix[2][2] = 1.5;
    // expected.rows[3] = Vector4{1.0, 2.0, 3.0, 1.0};

    // ASSERT_EQ(result, expected);
}